import { drawing as d, geometry as g } from '@progress/kendo-drawing';
import { TextBlock } from "./TextBlock";
import { getSpacing } from './utils';
import { Paragraph, TextRun, InlineImage, ParagraphMargin } from './../types';
type ResolvedParagraph = Paragraph & {
    margin: ReturnType<typeof getSpacing>;
};
/** @hidden */
export declare function getParagraphs(options: any): ResolvedParagraph[];
/** @hidden */
export declare const richTextRect: (options: any) => g.Rect;
type VisualNode = {
    type: 'text';
    text: string;
    run: TextRun;
    width: number;
    height: number;
    baseline: number;
    font: string;
} | {
    type: 'image';
    run: InlineImage;
    width: number;
    height: number;
    baseline: number;
};
export declare class RichTextBlock extends TextBlock {
    drawingElement: d.Group;
    alignable: boolean;
    readonly name: string;
    _textOnlyRect?: g.Rect;
    content(contentOptions?: {
        blocks?: Paragraph[];
        margin?: number | ParagraphMargin;
    }): any;
    redraw(options: any): void;
    _initText(): void;
    _wrapParagraphs(paragraphs: Paragraph[], maxWidth?: number): VisualNode[][][];
}
export {};
