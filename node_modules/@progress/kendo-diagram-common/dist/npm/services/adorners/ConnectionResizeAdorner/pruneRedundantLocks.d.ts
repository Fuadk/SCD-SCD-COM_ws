import { Lock, ConnectionBuildOptions } from '../../routing/CascadingRouting';
type PruneOptions = {
    tolerance?: number;
};
export declare function pruneRedundantLocks(locks: Lock[], buildOpts: ConnectionBuildOptions, pruneOpts?: PruneOptions): Lock[];
export {};
