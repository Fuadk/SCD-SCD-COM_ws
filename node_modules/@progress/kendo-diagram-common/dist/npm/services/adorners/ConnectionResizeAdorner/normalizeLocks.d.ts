import { Point } from '../../../math';
import { type Lock } from '../../routing/CascadingRouting';
/** removes locks that don't change anything (applyLock gives the same point) */
export declare function normalizeLocks(locks: Lock[], connectionStart: Point): Lock[];
