import { Point } from '../../../math';
import { Circle } from '../../../svg';
import { AdornerBase } from '../AdornerBase';
import { type Connection } from '../../../dom/Connection';
import { ConnectionDragUnit } from '../../ConnectionDragUnit';
import { ConnectionPointsEditable, DiagramDomEvent } from '../../../types';
interface ResizeHandle {
    visual: Circle;
    point: Point;
}
export declare class PolylineConnectionResizeAdorner extends AdornerBase {
    connection: Connection;
    handles: ResizeHandle[];
    activeHandle: ResizeHandle | null;
    _initialPoints: Point[] | undefined;
    _firstMove: boolean;
    _handlePoints: {
        point: Point;
        vertex: boolean;
    }[];
    constructor(connection: Connection, options: boolean | ConnectionPointsEditable);
    onDblClick(e: DiagramDomEvent): void;
    createHandlePoints(): {
        point: Point;
        vertex: boolean;
    }[];
    private _createHandles;
    _getCursor(point?: Point): string | undefined;
    _hitTest(point: Point): ResizeHandle | null;
    start(p: Point): void;
    move(_handle: ResizeHandle, p: Point): boolean;
    private updateConnection;
    /**
     * Apply snap to point position based on adjacent points
     * @param p - The current point position
     * @param handlePointIndex - The index of the handle being dragged
     * @returns The snapped point position
     */
    private _applySnap;
    stop(_p: Point): ConnectionDragUnit;
    refresh(): void;
    cancel(): void;
    _resetFlags(): void;
}
export {};
