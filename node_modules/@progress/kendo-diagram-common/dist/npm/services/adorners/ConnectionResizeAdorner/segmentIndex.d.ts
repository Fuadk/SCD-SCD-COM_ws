import { Point } from '../../../math';
import { SegmentMeta } from '../../routing/RouteResult';
type HitOpts = {
    tolerance?: number;
};
export declare function findSegmentIndexFromHandlePoint(handle: Point, segmentsMeta: SegmentMeta[], opts?: HitOpts): number;
export {};
