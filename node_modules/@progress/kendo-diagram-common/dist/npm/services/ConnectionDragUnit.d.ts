import { Lock, Side } from './routing/CascadingRouting';
import { type Connection, type RoutingMode } from './../dom';
import { type Point } from './../math';
export interface ConnectionDragUnitOptions {
    item: Connection;
    undoLocks: Lock[];
    redoLocks: Lock[];
    undoPoints: Array<Point>;
    redoPoints: Array<Point>;
    routingMode: RoutingMode;
    sourceSide: Side | null;
    targetSide: Side | null;
}
export declare class ConnectionDragUnit {
    item: Connection;
    _undoLocks: Lock[];
    _undoPoints: Array<Point>;
    _undoRoutingMode: RoutingMode;
    _undoSourceSide: Side | null;
    _undoTargetSide: Side | null;
    _redoLocks: Lock[];
    _redoPoints: Array<Point>;
    _redoRoutingMode: RoutingMode;
    _redoSourceSide: Side | null;
    _redoTargetSide: Side | null;
    title: string;
    constructor(options: ConnectionDragUnitOptions);
    undo(): void;
    redo(): void;
    _applyConnectionLocks(options?: {}): void;
    _updateConnection(points: Point[]): void;
}
