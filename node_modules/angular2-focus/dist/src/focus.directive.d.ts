import { ElementRef, SimpleChanges, OnInit, OnChanges } from '@angular/core';
export declare class FocusDirective implements OnInit, OnChanges {
    focus: boolean;
    private element;
    constructor($element: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    focusElement(): void;
}
