import { AnyString } from '../../type/any-string.type';
import { ResultCallback } from '../../type/result-callback.type';
import { StringOfLength } from '../../type/string-of-length.type';
export declare const guardStringLength: <Type extends AnyString, Length extends number, Payload extends object = object>(value: Type, length: Length, callback?: ResultCallback<Type, {
    length: Length;
} & Payload> | undefined, payload?: Payload | undefined) => value is StringOfLength<Length, Length, Type>;
