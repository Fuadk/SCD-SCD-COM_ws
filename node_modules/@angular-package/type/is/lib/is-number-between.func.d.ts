import { AnyNumber } from '../../type/any-number.type';
import { NumberBetween } from '../../type/number-between.type';
import { ResultCallback } from '../../type/result-callback.type';
export declare const isNumberBetween: <Type extends AnyNumber = number, Min extends number = number, Max extends number = number, Payload extends object = object>(value: any, min: Min, max: Max, callback?: ResultCallback<any, {
    min: Min;
    max: Max;
} & Payload>, payload?: Payload | undefined) => value is NumberBetween<Min, Max, Type>;
