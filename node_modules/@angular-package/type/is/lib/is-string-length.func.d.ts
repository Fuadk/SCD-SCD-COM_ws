import { AnyString } from '../../type/any-string.type';
import { ResultCallback } from '../../type/result-callback.type';
import { StringOfLength } from '../../type/string-of-length.type';
export declare const isStringLength: <Type extends AnyString = string, Length extends number = number, Payload extends object = object>(value: any, length: Length, callback?: ResultCallback<any, {
    length: Length;
} & Payload>, payload?: Payload | undefined) => value is StringOfLength<Length, Length, Type>;
