import { AnyString } from '../../type/any-string.type';
import { ResultCallback } from '../../type/result-callback.type';
import { StringOfLength } from '../../type/string-of-length.type';
export declare const isStringLengthBetween: <Type extends AnyString = string, Min extends number = number, Max extends number = number, Payload extends object = object>(value: any, min: Min, max: Max, callback?: ResultCallback<any, {
    min: Min;
    max: Max;
} & Payload>, payload?: Payload | undefined) => value is StringOfLength<Min, Max, Type>;
